function checkWebViewId(webviewId) {
  if (!webviewId) {
    console.log('webviewId is not set.');
    // Additional code to handle the falsy case
  } else {
    console.log('webviewId is set to:', webviewId);
    // Code to execute when webviewId is truthy
  }
}
